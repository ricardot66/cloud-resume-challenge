# AWS SDK (usually included in Lambda runtime, but good to specify version)
boto3>=1.26.0

# Common Lambda dependencies (add only what you actually use)
# requests>=2.28.0        # If you make HTTP requests
# pandas>=1.5.0          # If you do data processing  
# numpy>=1.24.0          # If you do numerical computing
# python-dateutil>=2.8.0 # If you handle dates

# Add your specific dependencies here
